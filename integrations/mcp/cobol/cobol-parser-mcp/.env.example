# Log verbosity for the host process using this MCP (if applicable)
LOG_LEVEL=info

# COBOL parser knobs
COBOL_DIALECT=COBOL85
# 0 disables the soft loop budget so we can finish a batch
COBOL_PARSER_BUDGET_SECONDS=0
# Hard cap (per Java invocation) so ProLeap/cb2xml can’t hang forever
COBOL_JAVA_TIMEOUT_SEC=12
# Source format hint passed to ProLeap bridge (FIXED or VARIABLE)
COBOL_SOURCE_FORMAT=FIXED

# Raw AST debug dumps (set to a mounted path to collect XML/attempt logs)
# RAW_AST_DUMP_DIR=/mnt/work/.renova/debug/raw-ast

# Optional: search paths for copybooks (not directly consumed; the adapter
# derives likely copybook folders from the source index)
COBOL_COPY_PATH=

# Encoding hints list (currently unused)
ENCODING_HINTS=[]

# Legacy; not required when using classpaths
PROLEAP_JAR=/opt/proleap/cb2xml.jar
