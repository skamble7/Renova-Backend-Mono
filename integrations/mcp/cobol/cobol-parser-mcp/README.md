# cobol-parser-mcp (stdio)

Deterministic MCP server that parses COBOL programs & copybooks and emits CAM-normalized artifacts.

## Run (local)

```bash
python -m src.main --stdio
# prints "mcp server ready"
